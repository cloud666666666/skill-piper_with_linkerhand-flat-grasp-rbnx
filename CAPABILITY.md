---
description: 用平掌包络方式抓取桌面上的蔬菜——`grab` 抓起并保持握持，`put_down` 把手里拿着的东西放回原处，`pick` 一次做完"抓起来再放回去"，`home` 把机械臂收回待机位（可选失能）。目标不在桌面上时明确报告"未检测到"，不会盲抓。
---

# 平掌抓取 skill（Piper 臂 + LinkerHand O6）

本 skill 用**灵巧手平掌包络**抓取桌面物体——掌心对准物体、四指从侧面合拢包住它，
不是夹爪式对夹。可识别类别来自部署的 YOLO 模型（`carrot` / `potato` / `tomato`）。

## `grab`

用户想**把东西拿起来并保持握持**时调用——"抓起来 / 拿起来 / 递给我 / 抓住那个番茄"。

```json
{"object_name": "tomato"}
```

`object_name` 直接传用户说的那个词（中英文都行）。检测服务会在它认识的类别里做子串匹配；
匹配不到会返回"未检测到"，把这个结果如实转告用户，**不要自己编造桌上有什么**。

抓成功后物体一直在手里，直到用户要求放下。

## `put_down`

用户想**把手里拿着的东西放下**时调用——"放下 / 放回去 / 松手 / 放桌上"。无参数：

```json
{}
```

降到抓取时那个点、张手、退回、归位。**放了才会松手**——不会在举着的高度直接松开，
也不会放到别的地方去。

**"放下"要用这个，不要用 `pick`。** `pick` 会重新检测、重新抓取，它在接近时会先张开手，
等于把手里原来那个丢掉再抓一次。手里有东西、用户说"放下"时，只调 `put_down`。

手里没东西时会返回 `nothing is held`，别把它当失败重试——那只是说明当前是空的。

## `home`

用户想**让机械臂收回去**时调用——"回零点 / 归位 / 回到初始位置 / 收工 / 停下 /
机械臂失能 / 断电"。

**两个字段都是必填**（schema 里标了 required），省略会报校验错误：

```json
{"target": "safe", "disable": false}
```

- `target`：`"safe"` → 示教待机位（启动时待的那个位置）；`"zero"` → 六关节全零
- `disable`：到位后是否关闭电机使能

两者 link6 都在 z≈0.21，远高于桌面，**都安全**。

**失能**（用户说"失能"、"松开电机"、"断电"时）：

```json
{"target": "safe", "disable": true}
```

这是**失能不是断电**——驱动器仍通电，随时可以重新使能，不用重启任何东西。
顺序不能颠倒，所以本工具总是先移动再失能；**移动失败时它不会失能**，
因为让一条停在意外位置的臂突然卸力比不归位更糟。

## `pick`

用户想**抓起来再放到别处**时调用——"拿一下再放回去 / 挪一下这个番茄 / 放到那边"。

```json
{"object_name": "tomato"}
```

行为与 `grab` 相同，抓起来之后会**放到该类别配置的放置点**。

**放置点在哪里配**：`robonix/robonix_manifest.yaml` 里 `flat_grasp` 的 `place_pos`。
按检测器的类别名索引，`pos` 是 `[x, y]`（arm/base_link，米），每个分量可以是数字，
也可以是相对物体当前位置的符号引用：

```yaml
place_pos:
  tomato: {pos: [0.30, -0.10]}     # 固定点：前 30cm、右 10cm
  potato: {pos: ["x", "-y"]}       # 相对：物体自身 x，y 取反
```

**没配的类别放回原处**（就是你抓它时的那个点）。

放好后机械臂自动回到待机位。

## 两个工具共同的注意点

- **调用会真的让机械臂动**，且整个序列约 20-30 秒（接近 → 下降 → 合手 → 抬起 → 复位）。
  在返回结果前不要重复发起同一个调用。
- **成功是有校验的**：闭环后 skill 会读灵巧手的位置反馈，确认手指确实被物体挡住。
  空手（手指完全合到底）会返回 `grasp_verify_failed`，**不是**成功——这种情况说明抓偏了，
  如实告诉用户没抓到，不要当成抓到了。
- 失败信息里通常带 `detection_failed` / `grasp_pose_failed` / `execute_grasp_failed` /
  `grasp_verify_failed` 前缀，指明是哪一段出的问题。
- 桌面位置换了之后抓取精度可能下降——这份手眼标定只在标定点覆盖的区域内可靠。
